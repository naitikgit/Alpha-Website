const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.links');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');toggle.setAttribute('aria-expanded','false')}))}document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());const filters=[...document.querySelectorAll('[data-filter]')];const cards=[...document.querySelectorAll('[data-category]')];const search=document.querySelector('[data-search]');let current='all';function filterProducts(){const q=(search?.value||'').toLowerCase().trim();let visible=0;cards.forEach(card=>{const show=(current==='all'||card.dataset.category===current)&&(!q||card.textContent.toLowerCase().includes(q));card.hidden=!show;if(show)visible++});const empty=document.querySelector('.empty-state');if(empty)empty.style.display=visible?'none':'block'}filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));btn.classList.add('active');current=btn.dataset.filter;filterProducts()}));search?.addEventListener('input',filterProducts);const form=document.querySelector('[data-contact-form]');form?.addEventListener('submit',e=>{e.preventDefault();document.querySelector('.form-message')?.classList.add('show');form.reset()});
